fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# Spatial statistics (api/spatial_stats.py): global Moran's I + Local Moran's I (LISA).
# Pulls in geopandas/shapely transitively (libpysal's own dependency), but confirmed
# installable via prebuilt wheels -- no GDAL/C-toolchain build step, no compilation -- on
# both this Windows dev machine and the Dockerfile's python:3.12-slim (Linux) base (real
# `docker compose build`, not assumed). This code path doesn't use geopandas directly --
# weights are built from a plain lat/lon array. See SPATIAL_STATISTICS.md.
libpysal==4.15.0
esda==2.10.0

# Per-prediction explainability (api/model_service.py): SHAP TreeExplainer over the shipped
# RandomForest. Exact algorithm, no sampling/seed needed -- confirmed bit-identical across
# repeated calls. See SHAP_EXPLAINABILITY.md for the ~45-55ms/row cost this carries and the
# speed-gated replay / on-demand-explain design that works around it.
shap==0.52.0

# --- Transitive pins added in Phase 3 (ops hardening) -------------------------
# The direct dependencies above were already pinned; these are the transitive ones fastapi,
# uvicorn[standard], libpysal and esda pull in. Pinned so a rebuild resolves the same tree
# rather than whatever is newest on the day -- a floating starlette in particular can change
# middleware and cookie behaviour, which Phase 3's auth depends on.
starlette==0.41.3
pydantic_core==2.27.2
annotated-types==0.8.0
typing_extensions==4.16.0
anyio==4.14.2
idna==3.19
sniffio==1.3.1
click==8.5.0
h11==0.16.0
colorama==0.4.6
httptools==0.8.0
python-dotenv==1.2.3
PyYAML==6.0.3
watchfiles==1.2.0
websockets==17.1

# libpysal/esda transitive set (geospatial wheels -- no C toolchain needed, confirmed on
# python:3.12-slim; see the note above these packages in this file).
geopandas==1.1.4
shapely==2.1.2
pyproj==3.7.2
pyogrio==0.13.0
platformdirs==4.11.7
requests==2.34.2
certifi==2026.7.22
charset-normalizer==3.5.1
urllib3==2.7.0
beautifulsoup4==4.15.0
soupsieve==2.9.2
Jinja2==3.1.6
MarkupSafe==3.0.3
