contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
joblib==1.5.3
kiwisolver==1.5.0
matplotlib==3.11.1
narwhals==2.24.0
numpy==2.5.2
packaging==26.3
pandas==3.0.5
pillow==12.3.0
pyarrow==25.0.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
scikit-learn==1.9.0
scipy==1.18.0
six==1.17.0
threadpoolctl==3.6.0
tzdata==2026.3

# --- Transitive pins added in Phase 3 (ops hardening) -------------------------
# Everything above was already pinned. These are the transitive dependencies that were
# previously resolved at install time and are now pinned too, so `pip install -r` produces
# the same tree that this repo's numbers were measured on. Captured from the environment the
# Phase 1-4 work ran in (Python 3.12.10, Windows 11), and confirmed installable from
# prebuilt wheels on the Dockerfile's python:3.12-slim base.
llvmlite==0.49.0
numba==0.67.0
cloudpickle==3.1.2
slicer==0.0.8
tqdm==4.70.0
